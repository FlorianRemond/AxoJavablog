<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.axojavablog">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AdapterArticle" />
        <activity android:name=".SupprimerActivity" />
        <activity android:name=".AjoutActivity" />
        <activity android:name=".GestionAdminActivity" />
        <activity android:name=".HomeAdminActivity" />
        <activity android:name=".ValidationActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".RegisterActivity" />
        <activity
            android:name=".validation"
            android:label="@string/title_activity_validation"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ReadActivity"
            android:label="@string/title_activity_read"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>